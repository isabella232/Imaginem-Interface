import ProcessedImages from '../components/ProcessedImages';
import React from 'react';

export const ResultsPage = React.createClass({
  render() {
    return (
      <div>
        <ProcessedImages />
      </div>
    )
  }
});
